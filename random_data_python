import random
import numpy as np
import pandas as pd


daten=random.randrange(start=3, stop=15, step=1)
random_data=np.random.choice(daten, size=(15,1))

df=pd.DataFrame(random_data)
df.reset_index
#df.to_csv("temp_dev1.csv", header=None)

print(df)



#help(random)
